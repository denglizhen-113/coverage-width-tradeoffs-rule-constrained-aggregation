# Code Deposit Mirror

This directory groups the two preregistered experiment entry points requested
for repository deposit:

- `scripts/26x1_multiseed_sensitivity.py`
- `scripts/26x2_baselines_ablation.py`

Their imported research modules are mirrored under `src/`. The canonical
runnable copies are at repository-root `scripts/` and `src/`, because the full
pipeline uses that standard project layout. The mirror files were copied
byte-for-byte during Stage 26AA; do not edit one copy without synchronizing and
rechecking both.

